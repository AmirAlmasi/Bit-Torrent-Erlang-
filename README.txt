Compile all the files and then in the erlang shell
Run the run.bat file 
type in "erbit_app:start(normal, []).". Then start ErBit_GUI.jar,
then in the GUI choose add a new torrent file and
then select a torrent file that you want to download.